 <div class="col s12 m7 newformat">
    <div class="card horizontal outstanding">
      {{#if photo}}
      <div class="card-image small">
        <img src="{{photo}}">
      </div>
      {{/if}}
      <div class="card-stacked">
        <div class="card-content">
         <a href="/art/{{id}}"> <p>{{title}}</p> </a>
        </div>
       
      </div>
    </div>
  </div>